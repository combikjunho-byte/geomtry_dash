[project]
name = "geometry-dash"
version = "0.1.0"
description = "A Geometry Dash clone built with pygame"
readme = "README.txt"
requires-python = ">=3.10"
dependencies = [
    "pygame-ce>=2.5.7",
]

[tool.uv]
package = false
